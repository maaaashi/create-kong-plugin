_format_version: "3.0"

services:
  - name: create-kong-plugin
    url: http://localhost
    routes:
      - name: test
        paths:
          - /test
        plugins:
          - name: example
          - name: example-go